# 变更记录

本文件记录**用户能观察到的变化**（配置字段、远端协议语义、CLI 表面、退出码、页面行为），
不记实现细节。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本语义见 [CONTRIBUTING.md](CONTRIBUTING.md#版本语义)。

## [Unreleased]

### 变更

- 隧道重连的退避序列（1s→2s→4s→8s→16s→30s 封顶）从「确定值」改成「以此为上界，
  实际在半程到满程之间随机取」。原先一起断的主机会锁着步一起重试：网络整体回来的那一瞬
  （合盖睡醒、Wi-Fi 切换、跳板机抖一下）它们同时敲门，被跳板机的 `MaxStartups` 挡掉之后
  又一起进下一档，永远散不开。实测 16 台同时断，峰值并发 12 条 ssh（出厂额度 10），
  有 6 台在 70 秒后仍卡在「重连中」，且重连次数完全相同。现在它们会自己散开。
  同时重连环也过扇出闸了——此前探测/恢复/自启/巡检都有闸，唯独最容易集体触发的这个环没有。

### 新增

- `dshc --version` / `dshc -V` 与 `dshc version` 同义。此前 `--help`/`-h` 都收，
  `--version` 却报「未知命令」退 3——排查现场问「你装的哪版」，第一反应就是敲它。
  （`-v` 留着没占，`--verbose` 迟早要个短名。）

### 修复

- 对一台根本没在跑的主机执行关停/重启，不再回「仅适用于本 manager 拉起的实例
  （不动手动实例）」。那台上面一个实例都没有，更没有什么手动实例，这句话把人往
  「是不是有个我不知道的进程」上引。现在两种情形分开说：真有不是我们拉起的实例才提
  手动实例，什么都没有就直说「上面没有本 manager 拉起的实例」。

- 主机从配置里消失之后，它的隧道不再赖着不走。手改 `config.json` 删掉一台正在跑的主机再
  `reload`（或者重跑一次首启引导——它是整份替换），页面上这台就没了，`stop`/`reconnect`
  一律回「未知主机」；可那条隧道 ssh 还活着、本机端口还在转发，而**没有任何入口能碰到它**。
  现在这半边由 manager 自己收掉。远端实例不动：删一条配置不等于「把远端那个服务停掉」，
  所以只在日志里说清它还在跑、pid 多少、想收拾就把主机加回来；state 记录也留着，
  主机加回来时还能接管它。

- 多台主机同时首次启动时不再分到同一个本机端口。分配是「算出哪些号已占用 → 试绑 → 回写
  config」，而试绑要等，等的这段里另一台读到的还是那份旧账——实测 8 台同时首启全都分到
  `17701`。这条路真机上就在走：`autoStart` 和恢复复核都是一次 6 台在飞，全新安装的那几台
  恰好都还没有端口号。后果不是「撞一次重试就好」：端口号是分配即写死的（要让 iframe 地址
  和浏览器书签稳定），撞号会被永久写进 `config.json`，先建起来的那条隧道占着号，其余几台
  每次都撞「端口已被占用」而挂起，提示还让人去找占端口的进程——占号的正是 manager 自己的
  另一条隧道。重启也修不回来，只能手改 `config.json`。现在整个分配串行，同一台被并发要
  两次也只分一个号。

- 一台狂吐输出的远端不再能撑爆 manager。此前每条一次性 ssh 的 stdout/stderr 是无上限地
  往一个字符串上拼——远端吐多少就吃多少：实测 256MB 输出把 manager 的常驻内存顶到
  359MB 且不回落，越过约 512MB 就是一个没人接得住的 `RangeError`，manager 当场死、
  所有隧道陪葬。真机上撞得到：`dsh web` 的日志里只要打过带 `\r` 的进度条（模型下载、
  pip、tqdm），那整段就是**一行**，而取日志只限了行数没限字节，用户点一下「看日志」就中；
  远端 `.bashrc`/motd 刷屏也走同一条路。现在每条流各留末尾 2MB：留尾是因为要的东西都在
  尾巴上——日志看的是最新几行，协议的 `KEY=VALUE` 也是脚本最后才打的，前面刷屏的是
  远端登录 shell。被截过会在开头写明丢了多少，不让人以为日志本来就长这样。

## [0.2.0-rc.5] - 2026-08-21

**预发布**。接着 rc.4 往下挖：交互面 2 轮（窄屏落点、蒙层叠加时的 Esc）
加工程稳定性 9 轮（进程孤儿、闸门自查、日志封顶、规模、扇出、磁盘不可写、长跑内存、安全面）。
这一批里**会让人丢东西或丢服务**的有三条：外部手改的 `config.json` 被静默覆盖、
磁盘写不进时 manager 直接死、关停留下没人收的 ssh。

### 修复

- `dshc up` / `dshc restart` 拉起失败时不再留下一个在后台跑的 manager。此前预算内没确认
  健康就只报一句「未确认健康」并退 2，那个进程照旧活着、照旧在试监听——等占着端口的人
  一走它就把端口接过去，于是有一个「启动失败过」的 manager 在服务，谁也说不清它是几时
  以什么配置起来的。现在没确认健康就把它收走（TERM→1s→KILL），文案分「已把它收走」与
  「它自己退了」两种说法，都指到 `manager.log`。
- `manager.log` 不再无限长。它只追加、从不回收，而 manager 在 launchd 下是 7×24 常驻的：
  健康态几乎不写，但一台链路不稳的主机（断联→退避→重连每一拍都记）实测约 8MB/天，
  放一年就是几个 G 躺在 `~/.dsh_center/`，谁也不会去看一眼。现在到 8MB 就原地截断、
  留最后 1MB，并在开头留一行说明丢了多少。截断只能原地做——日志是被继承的 O_APPEND fd
  在写，改名换文件的话进程还往原来那个 inode 里写，用户看到的新文件永远是空的。
- demo 冒烟不再偶发红在「mock 页没被 iframe 加载」。那条判据原先是一次性的：`iframe`
  元素刚建出来就去翻已收到的响应，可 `src` 刚设上、请求还在路上——本机快看不出来，
  CI 的 runner 一慢就翻空。现在它是有上限的等待，超时才红，且错误里点明等的是什么。
- 请求体超过 1MB 上限时回的是 `400 请求体超过 1048576 字节上限`，不再是一个断掉的连接。
  此前服务端判得没错、内存也没被撑爆，但它在把 400 写出去之前就把 socket 拆了，对面只
  看到 `ECONNRESET`／`EPIPE`，页面上表现为「网络错误」，日志里也留不下线索。现在超一点点
  会替对面读完再回话（额度 4MB，够覆盖「值填大了」这种形态）；超过额度就是在灌，照旧掐掉，
  manager 的内存不跟着涨。
- 磁盘写不进去时 manager 不再当场死。运行状态是每 100ms 攒一批落盘的，这个落盘在定时器
  回调里——写不进就是未捕获异常，进程直接没，所有隧道陪葬，launchd 还会把它拉起来接着死，
  成翻滚循环。触发它不需要人为作妖：`~` 所在卷写满、外接/网络盘转只读、`sudo` 跑过一次
  之后目录属主变 root，都够了。现在写不进只记一条人话（写的是哪个文件、常见成因是什么、
  后果是「重启后回到上次写成功的那份」），manager 照常管隧道，等磁盘又能写了自己补上，
  并再报一条「又能写了」。同一个毛病反复出现只报一次，不刷屏。
- `~/.dsh_center` 不可写时 `dshc up` 给的是人话不是 Node 栈。此前 pidfile 与 manager.log
  两处写失败都直接把 `EACCES: permission denied, open '/Users/.../manager.pid.tmp.51096'`
  连栈摔出来；现在是「manager 的运行记录写不进去，没法启动」外加文件路径与三条常见成因。
- 「对每台各来一次 ssh」的动作（全量探测、启动时的恢复复核、autoStart、巡检深复核）
  现在最多同时开 6 条，不再有多少台就开多少条。远端 sshd 的 `MaxStartups` 出厂值是
  `10:30:100`——未完成认证的连接过 10 条就开始被随机丢，而多台远端共用一台跳板机
  （`ProxyJump`）时这个额度是合起来算的。实测 40 台、100 台的峰值就是 40、100 条，
  真机上会有随机几台以 `kex_exchange_identification: Connection closed by remote host`
  收场、在页面上显示成不可达，刷一次换几台。代价是主机很多时全量探测不再是「一瞬间
  齐刷刷变绿」而是分批推进（100 台、每台 1 秒往返约 17 秒），期间那些还没轮到的显示
  未探测，对它们的启动请求会被拒（`PHASE_CONFLICT`），探完即恢复。
- 假远端装置的状态文件改成原子落盘，规模向的用例不再随机红。它此前直接覆盖写
  `state.json`，而垫片的 POLL / VERIFY 是在锁外读的（读一眼不值当加锁）：正好读到写了
  一半的 JSON 就解析失败、被吞成空状态，于是 VERIFY 报「进程已消失」，一台活得好好的
  主机被回滚回 `ready`。40 台并发拉起实测每 3～6 轮中一次，且伪造出的是产品侧根本不存在
  的故障。
- 质量闸门会点用例的名了：声明多少个就必须跑了多少个，差一个就红并指出是哪个文件。
  起因是 `tests/integration/cli.test.js` 里一条用例把自己进程 TERM 掉了（无参
  `dshc restart` 是「重启 manager」，它照约定给 pidfile 里的 pid 发信号，而那正是用例
  进程自己），文件从此截断——后面十个用例压根没跑，`node --test` 却把这个文件报成通过，
  本地与 CI 一路全绿。

- 抽屉里有未保存改动时按 Esc 不再毫无反应。「放弃未保存的修改？」是原生 `<dialog>`，
  `showModal()` 就在这一记 Esc 的处理器里调，浏览器随后处理同一记 Esc 的默认动作，一眼
  看到刚开的模态框就把它关了——抽屉不关、框也不留，只能改用鼠标。现在开框的那一记会把
  默认动作摘掉；而框已经开着时 Esc 交回给框自己，等价于点「取消」，草稿照旧留着。
- manager 退出时不再留下没人收的 ssh 子进程。此前只有隧道那条常驻 ssh 会被关掉，正在
  路上的一次性 ssh（探测 / 拉起 / 回读）全被交给 init 当孤儿：`dshc restart` 之后新老两批
  命令同时打同一台远端，远端能看到两个 `dsh web` 抢同一个端口。现在关停会把在飞的那批按
  同一条 TERM→2s→KILL 收走，并且从此不再起新的——不落这道闩的话，每主机队列里排着的下一个
  任务会立刻顶上来，退出过程会一直冒新 ssh。
- 关停正好撞上退避重连的那一拍时，不会再多冒一条隧道 ssh。重连前要等一次远端复核，这段
  等待里 manager 可能已经关完了，醒来后不复查就直接重建隧道——那条 `ssh -N -L` 没人再来收，
  本机端口被它一直占着，新起来的 manager 只能另换端口，页签地址跟着变。
- 右键菜单不再落到视口外：靠右/靠下打开时按自身尺寸朝反方向翻，两边都装不下就贴边。
  菜单是 `position: fixed`，越出去那一截没有滚动可言——实测 1440×900 下右键最右边的标签，
  菜单越界 177px，末两项鼠标压根点不到。
- 右键菜单不再被 toast 压住。`--z-menu` 排到 `--z-toast` 之上（仍在对话框之下）：亲手唤出的
  菜单不该被一条自己会消失的通知挡住。另外 `.toast-region` 不再吃指针事件，多条 toast
  之间的间隙也不会白白吞掉一次点击。
- manager 跑着时被外部改过的 `config.json` 不会再被静默覆盖。此前 manager 只在启动时读
  一次配置，之后任何一次写配置（`dshc config set`、向导、加删主机）都是整份落盘，会把这
  期间手改的行悄悄抹掉——文件还在、时间戳还新，改动却没了。现在落盘前先核对磁盘上那份是不
  是自己上次见到的样子，对不上就整份拒写，报 `CONFIG_STALE`（HTTP 409）并直接印出「`dshc
  restart` 让 manager 读磁盘上的版本」，不用加 `--verbose` 才看得到。
- 安装脚本遇到不认识的参数当场报用法错误并退 3，不再静默忽略。把 `--standalone` 拼成
  `--no-git` 的人本意是「别走 git」，静默忽略的后果是他拿到 `release` 分支上的旧版本、
  还以为同时给的 `--pre` 生效了；把 `--service` 拼成 `--srevice` 的人以为装了开机自启，
  实际什么都没装。`install.sh` 与 `scripts/install.mjs` 两侧都认。
- 配置读不了时的提示不再把错误码印两遍（`EACCES EACCES: …`）。
- 参数写错的命令统一按用法错误退 3（此前 `dshc start` / `stop` / `restart` / `log` 漏参数时
  印的明明是用法行，退出码却是 1，和「操作失败」撞在一起——脚本按退出码分流的人会拿去重试）。
  命令行上给了非法值也归用法错误（与 `dshc up --port` 越界同一口径），并且把「哪个字段、
  要什么」当场印出来，不再要求补一个 `--verbose` 重敲一遍。
- 主机表格不再在按住的那一瞬重建，把这一次点击或空格整个吞掉。鼠标与 Space 的原生激活
  都在「抬起」那一刻，且要求按下与抬起落在同一个 DOM 节点上；而这张表每收到一条主机变化
  就重建对应行，一次重建插进按下与抬起之间，用户的体感就是「按了没反应」。现在按压期间把
  重绘攒起来，松手后再刷（攒的窗口是亚秒级）。Enter 一直不受影响——它在按下时就激活。
- 本机装着预发布版时，`dshc update` 除了报「已是最新」还会点出更新的预发布。此前跟着
  rc 的人会被卡在旧 rc 上：正式版比 rc 旧，稳定口径正确地不降级，于是只说「已是最新」，
  更新的 rc 整个被藏掉，也没人会想到要加 `--pre`。装正式版的人不受打扰。

## [0.2.0-rc.4] - 2026-08-21

**预发布**。这一轮全是本机实测挖出来的：交互面 8 轮（键盘、焦点、断线恢复）
加工程稳定性 9 轮（安全、内存、进程句柄、性能、资源边界、并发、规模、崩溃一致性、XSS）。

### 安全

- 本机 HTTP 面加了两道跨站闸。「只绑 127.0.0.1」挡的是网络上的别人，挡不住用户自己的
  浏览器替别人发请求：随便一个网页就能 `fetch('http://127.0.0.1:<port>/api/hosts/x/start')`，
  简单请求不触发预检、浏览器照发，读不到响应但副作用照发生——实测里一个陌生 origin 的
  页面真把远端会话拉起来了。现在带 `Origin` 的请求必须来自 manager 自己的 origin
  （403 `FORBIDDEN_ORIGIN`），`Host` 必须是环回名（403 `FORBIDDEN_HOST`，静态页一并挡，
  防的是「攻击者域名解析到 127.0.0.1」这种把页面装进他自己 origin 的做法）。
  命令行不带 `Origin`，照常放行。

### 修复

- 配置文件损坏不再被当成「尚未初始化」。此前 `readConfigFile()` 把「文件不存在」「JSON 坏了」
  「权限读不了」一律吞成同一种情形：非交互下报「请先执行 dshc init」，而交互终端上
  `dshc up` 会**直接以 force 走向导**，整份替换掉那份只是被截断的配置——localPort 分配、
  workdir、注入的环境变量与 extraArgs、patch 清单一起没了。现在三种情形各说各话：不存在
  才提初始化；坏了就说损坏、指出文件、给「先手工修，或备份后 init --force」的出路；
  读不了就说权限。`dshc init --force` 覆盖损坏文件前会先备份成 `config.json.bad-<时间戳>`。
  （`state.json` 坏掉的既有行为不变：manager 照常启动，那是可重建的派生数据。）

- 配置写盘失败时不再出现「三种说法」。此前是先把新配置装进内存、再落盘：盘写不进去
  （目录只读、磁盘满、卷被卸载）时请求报 500、用户以为没生效，可跑着的 manager 已经在用
  新值，重启后又从盘上读回旧值静默回退。现在先落盘、成了才换内存，失败则配置一动不动，
  也不再广播「配置变了」。错误也从直接吐 `EACCES: permission denied, open '<绝对路径>'`
  改成一句人话（`CONFIG_WRITE_FAILED`：配置没能写入磁盘，本次修改已放弃），原始错误进
  detail。

- 单条超长日志行不再能把 manager 顶上去。环形缓冲只按条数限长（200 条），对「每条 8MB」
  毫无约束——远端打几条巨行（base64 blob、一整坨 JSON、带 traceback 的堆栈）就够了，
  实测 20 条 8MB 让堆从 9MB 涨到 176MB，且每条都会原样推给每个页面、页面再塞进 DOM。
  现在 msg 截到 2000 字、detail 截到 16K 字，截断处注明原文多长。

- 页面在但**不读**的时候（标签被系统冻结、笔记本合盖、网络黑洞），manager 的内存不再
  随日志条数一路往上。这类连接的帧只能堆在内存里排队，而 manager 是 launchd 拉着的
  常驻进程：实测一个不读的客户端 + 20000 条 1KB 日志，堆从 8MB 线性涨到 56MB，GC 收不回
  （无客户端的对照组恒定 8MB），照这个势头下去只是迟早 OOM，而没人会想到根因是「某个
  后台标签没在读」。现在积压持续超过 4MB 就断开那条连接（瞬时超线不算——日志成串来，
  读得正常的客户端也可能一个 tick 里被灌进几 MB、随即排空），再离谱的一次性积压有 32MB
  硬顶。被断开的页面照原有断线重连逻辑自己回来，首帧是完整快照，状态照样对得上；
  事件里会留一条 warn 说明发生了什么。

- manager 重连后，断线期间在飞的写操作不再让那一行一直禁着。202 受理的动作靠
  `operation-done` 帧结算，而那帧是在页面失联期间发出的、永远收不到；重连后的快照里
  主机明明已经在运行，按钮却要禁到动作超时，末了还多弹一条假的「超时」。现在快照会
  按终态结算这些动作，仍在中间态的不动。

- 标签右键菜单在真浏览器里恢复可用：方向键、Home、End 此前一个都不动（取到的是
  NodeList，代码却按数组用 `indexOf`，处理器当场抛错），纯键盘只能选中第一项；
  选完一项后菜单收起、焦点也不再掉回文档顶端，而是回到开它的那个标签。

- 首启向导用键盘走不再每一步都把焦点丢回文档顶端。四步之间整块面板是重建的，按下
  「下一步」的那个按钮随即消失，焦点无处可去；进第 3 步还要异步拉候选、每台探测完
  各渲一次，把刚安放好的焦点又抹掉。现在换步后焦点落在新步骤的标题上（读屏先念
  「第几步·做什么」），同一步内的重渲染认回原来的勾选框，向导收尾后焦点交给管理台标题。

- 事件面板的「按主机过滤」下拉现在也认单条到达的新主机。此前它只在整份快照时重建，
  以 `host-changed` 单帧出现的主机（ssh config 里多出一台、向导收尾解门禁都走这条）
  在下拉里缺席，事件看得见却筛不出来，要等 SSE 重连或刷新才补上。

- 主机行里的按钮与开关，用键盘按 Enter/Space 现在真按得动。行本身也接 Enter/Space
  （用来开配置抽屉），而它的 `preventDefault` 连带取消了行内控件的原生激活——键盘用户
  按「探测」「拉起」「关停」只会开抽屉，请求一个都发不出去；鼠标点却一切正常，因为那条
  路上控件各自 `stopPropagation`，压根到不了行。现在行只认落在自己身上的按键。

- 直接打开或刷新某台主机的页面，不再被踢回管理台。书签、浏览器刷新、
  `dshc open <host>` 走的都是「首屏就带主机路由」这条路；此前主机集合还没同步到，
  页面把「查不到这台」当成「标签已消失」，静默把地址改回管理台，深链因此永远落不下来。
  现在等主机集合到过一次再判；真的从配置里消失的主机，照旧回管理台。
- 安装脚本的输出接到 `head` 之类会提前关掉管道的命令时，不再刷一屏 Node 栈。
  软链本来就在报错前建好了，那屏栈只会让人误以为装坏了；现在安静收场并把活干完。
- 一键安装收尾的「下一步」不再印两遍（`install.sh` 与 `scripts/install.mjs` 各一份，
  说法还不完全一致）。直接跑 `scripts/install.mjs` 的人照旧看到它自己那份。
- manager 没起时，所有命令给同一句人话（`manager 未在 127.0.0.1:<端口> 运行。先执行 dshc up。`）
  并退 2。此前 `dshc open` 与 `dshc config set` 会把 `ECONNREFUSED` 和内部错误码
  `(INTERNAL)` 原样甩出来，同一件事两套说法。
- `dshc open` 在 manager 没起时不再打开浏览器：以前它退 0 还真拉起一个必定打不开的
  页面，让人去怀疑浏览器和端口。首启引导模式下照旧放行——那个页面就是向导本身。
- 主机多、名字长把标签栏撑过一屏时，切到靠后的那台会自动把激活标签滚进可视区。
  此前标签栏从不跟着走，切过去只看到一排没有高亮的标签，得自己横向拖回来
  （macOS 上滚动条是隐形的，不一定看得出能拖）。
- `dshc up --port` 越界（不在 1024–65535）当场报用法错误并退 3。此前会真去拉起一个
  必然绑不上端口的 manager，等满 10 秒健康检查，最后报「未确认健康」，
  把人往权限和 launchd 的方向带。
- 主机抽屉开着时，Esc 在任何焦点位置都能关掉它。此前 Esc 只在焦点还在抽屉里时管用，
  而抽屉是可以被 Tab 出去的——纯键盘用户一旦 Tab 到外面，就只能一路 Shift+Tab 摸回去
  或找那个 `×`。
- 抽屉开着时后面的页面不再吃键盘焦点。遮罩本来就挡住了鼠标，键盘却能一路 Tab 到被它
  压住的按钮上，焦点环画在灰蒙蒙的遮罩底下（`aria-modal` 还声称自己非模态，
  读屏用户拿到的是相反的信息）。现在按「有遮罩即模态」统一：后景 inert，
  `aria-modal="true"`。
- 主机表的状态更新不再把键盘焦点扔回文档顶端。这张表在任何写操作与任何主机变更时都会
  整片/整行重建，旧节点带着焦点被移除，浏览器只好把焦点交回 `body`——按下「拉起」的
  那一刻就失去位置，启动要走 5 拍每拍都重建，既跟不到变化也回不去；连「更新的是另一台」
  都会把当前这台的焦点掀掉。现在同一控件还在就留在它上面，控件因状态变化消失或在忙碌态
  被禁用，就退到它所在的那一行。
- 抽屉表单的错误提示现在跟着输入走：离开某个字段就报它自己的校验结果，改对了红字
  立刻灭。此前提示只在点保存时算一次、之后再没人碰——把端口改回合法值，红字和
  `aria-invalid` 还挂在那儿（读屏用户听到的仍是「无效」）；反过来，没点过保存之前
  填 `abc`、`0`、`70000` 一律没有任何提示。打字过程中不报——刚敲下一个 `0` 就红一次
  是纯噪声。

### 文档

- PR 标题格式的 CI 校验（CONTRIBUTING 已定约定，尚未机制化）留作待办。
- `operation-done` 事件帧尚未携带错误码，因此经 SSE 结算的动作失败一律是退出码 1，
  即使根因是远端超时。补了 `code` 之后 CLI 才能把这类失败映射成 2。

## [0.2.0-rc.3] - 2026-08-21

**预发布**。内容同 `v0.2.0-rc.2`，外加一处真机验收挖出来的修复。

### 修复

- 探测远端时不再把**探测自己**记成手动实例。远端 `ps` 里执行探测脚本的那层 `sh -c`，
  命令行中同时含有 `dsh` 与 `web` 字样，会被脚本自己的 `grep` 命中，于是每探一次就凭空
  多出一条「手动实例」（页面主机表恒显「另有 1 个手动实例」，连没装 dsh 的主机也显示）。
  真实的手动实例检测不变，关停判据从未受影响（它只认自管 pid + 逐字指纹）。

## [0.2.0-rc.2] - 2026-08-21

**预发布**，拿给人试装的版本：把「构建产物 / 一键安装 / 更新 / tag 自动构建」四件缺失
能力补齐。稳定用户 `dshc update` 更新不到它（`--pre` 才看得见）。

内容与未能出炉的 `v0.2.0-rc.1` 相同，外加一处发版流水线自身的修复：`rc.1` 的构建与
双架构验证全绿，最后一步建 Release 时 `gh` 因所在 job 没有 `.git` 而认不出仓库，
于是那个 tag 没有对应的 Release（仓库里能看到这个空 tag，属预期）。

### 新增

- **`dshc version`**：一条命令说清「装的是哪个版本、怎么装的、用的哪个 Node」——
  软件版本、安装通道（git 提交 / bundle 版本与架构）、Node 运行时版本与路径、
  安装位置。`--json` 给脚本读。
- **`dshc update`**：按安装通道自更新。git 安装走 `origin/release` **只快进**
  （工作区脏、或目标不是当前提交的后代，都拒绝并说清原因，不用 merge 糊过去）；
  发布包安装从 GitHub Releases 取版本，**SHA256 校验通过才落盘**，换目录是
  「解包到 `.new` → 原子改名」，上一版留在 `<安装目录>.prev` 可手动换回。
  `--pre` 才看得见预发布版本，`--ref <分支|tag>` 点名目标，`--restart` 顺带重启
  manager（默认只提示——重启会瞬断所有隧道页签，时机该由人挑）。
- **双架构发布包**（`npm run build:bundle`）：mac arm64 与 x64 各一份 tar.gz，
  **自带官方 Node 运行时**（下载时逐字节核对 nodejs.org 的 SHASUMS256.txt）。
  包内是 `bin/dshc` 启动器 + `runtime/` + `app/` + `BUNDLE_INFO.json`；`app/` 的内容
  就是打包白名单（`PACK_RULES`），不另立一份。
- **`install.sh` 新增 standalone 通道**：没装 node（或版本过低）的 mac 上**自动降级**
  到发布包安装，一条 curl 即可，全程不碰本机的 node；`--standalone` / `--git` 可强制，
  `--pre` 允许预发布，`--version <tag>` 钉死某个 Release。下载物 SHA256 校验不过就拒装。
  Linux 仍要求自带 Node ≥ 22（不发 Linux 包）。

### 变更

- `scripts/install.mjs` 认得出发布包安装：软链指向包内 `bin/dshc` 启动器而非
  `app/src/cli.js`（装的人可能压根没装 node，直接跑 cli.js 会找不到解释器），
  `--service` 也经启动器执行，好让 launchd plist 写上**自带**运行时的路径。
  通道判据与 `dshc update` 共用一份实现，不各写一遍。
- 打包白名单新增 `scripts/install.mjs`（发布包里要靠它摘链与装服务）。
- **打 tag 即出双架构产物**：发版流水线改成 build → verify → release 三段。产物必须先在
  Apple Silicon 与 Intel 两种真机上、用包内自带的 node 跑通 `dshc version` 才配挂到
  Release 上；verify 不过就不建 Release，不留半个发版。Release 附件是 2 个 tar.gz +
  `SHA256SUMS`。
- **发版 tag 支持预发布形态**（`v0.2.0-rc.1`）：守卫对 rc 只要求「出自 main」，不要求
  打在 `release` HEAD 上——rc 不动稳定指针；Release 自动标 Pre-release，因此
  `dshc update` 默认更新不到它，`--pre` 才看得见。正式版口径一字未改。

## [0.1.0] - 2026-08-21

首个版本。本机 manager + `dshc` CLI，把散在多台远端主机上的 `dsh web`
经 `ssh -L` 隧道收进同一个管理台页面。

### 新增

- **主机纳管**：主机清单来自 `~/.ssh/config`（`DSHC_SSH_CONFIG` 可换文件）；
  逐台探测分三类（可拉起 / 未装 dsh 或缺 web profile / 连不上），不装的主机
  明确标注原因而不是假装能用。
- **生命周期命令**：`dshc init / up / down / restart / status / logs /
  service install|uninstall|status`；主机操作 `dshc ls / probe / start / stop /
  reconnect / log / open / config`。退出码 `0` 成功、`1` 操作失败、
  `2` 超时或通信失败、`3` 用法错误。
- **单入口管理台**：每台在跑的主机占一个标签页，内容是真实的远端 dsh web；
  切标签只改显隐、不重载 iframe，页面内会话状态不丢。首启四步引导。
- **断联自愈**：隧道断开按 1/2/4/8/16/30s 退避重连；30s 一轮巡检先在本机对转发
  通道探活（要求真收到字节），不通才经 ssh 深复核远端，真死了才标 `crashed`。
  远端明确禁止转发（`AllowTcpForwarding=no`）时挂起，不进无意义的退避环。
- **manager 重启不重拉远端**：复核指纹接管原进程，只重建隧道。
- **每主机启动目录**：`hosts.<主机>.workdir` 指定远端 `dsh web` 的进程工作目录
  （即 dsh 的默认 workspace 根与 `AGENTS.md` 加载位置）。只收绝对路径或 `~`、
  `~/…`；留空即远端家目录；目录进不去时拉起明确失败，不悄悄退回家目录。
- **每主机注入项**：环境变量、追加参数、patch 文件同步。改动一律**下次拉起生效**，
  不动正在跑的实例。
- **一键安装**：`curl … | bash` 引导脚本（检查 git 与 node ≥ 22 → clone 到
  `~/.dsh_center/app` → 软链 `dshc` 进 `~/.local/bin`），重跑即升级；
  `--service` 顺带装 launchd 自启，`--prefix` 换软链落点。
- **项目主页与在线 demo**：GitHub Pages 站点，demo 跑的是产品前端本体，
  后端换成浏览器内的假 manager（状态迁移复用 `src/lib/machine.js`）。

### 变更

- 退出码 `2` 现在名副实：除了「连不上 manager」，远端 ssh 超时与不可达
  （`SSH_TIMEOUT` / `SSH_UNREACHABLE`）也归 `2`（此前落到 `1`）。动作被受理后
  失败（校验不过、相位冲突、拒杀、端口用尽、拉起失败）仍是 `1`。
- 一键安装默认装 `release` 分支（只有发过版的提交）。跟主干用 `DSHC_REF=main`，
  钉死版本用 `DSHC_REF=v0.1.0`。

### 安全

- **不误杀**：关停远端进程前逐字比对 `ps` 命令行指纹，对不上一律拒杀——
  别人手动起的实例只读不写，最坏情况是「该关的没关掉」而非「关错了别人的」。
- manager 只绑 `127.0.0.1` 且**无鉴权**，按内网单人桌面工具使用；注入的环境变量
  与追加参数在远端 `ps` 里可见，别往里放密钥。安全边界详见 README。
- **远端零常驻、零安装**：探测、拉起、关停全靠单条一次性 `ssh` 命令，远端只留
  `~/.dsh_center_remote/` 下的日志与可选 patch 文件。

### 文档

- 中英双语 README，界面截图由 `npm run site:shots` 自动生成，跟着代码走。
- `CONTRIBUTING.md`（分支 / PR / CI / 发版 / 修复 / review 全流程规矩）、
  `AGENTS.md`（改代码前的硬约束速查）、本变更记录。

[Unreleased]: https://github.com/shendeguize/Remote_DSH_Center/compare/v0.2.0-rc.3...HEAD
[0.2.0-rc.3]: https://github.com/shendeguize/Remote_DSH_Center/releases/tag/v0.2.0-rc.3
[0.2.0-rc.2]: https://github.com/shendeguize/Remote_DSH_Center/releases/tag/v0.2.0-rc.2
[0.1.0]: https://github.com/shendeguize/Remote_DSH_Center/releases/tag/v0.1.0
